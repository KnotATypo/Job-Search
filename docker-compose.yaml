services:
  app:
    image: knotatypo/job-search:latest
    ports:
      - '3232:3232'
    environment:
      S3_ENDPOINT_URL:
      S3_KEY_ID:
      S3_ACCESS_KEY:
      APP_SECRET_KEY:
      DATABASE_USER:
      DATABASE_PASSWORD:
      DATABASE_HOST:
      OLLAMA_HOST:
      SUMMARY_PROMPT:

volumes:
  listing-data:
